/**
 * The two series every finance chart in Navio uses, in this order, always.
 *
 * Checked against this product's panel surface for normal vision and for the
 * three common colour-vision deficiencies — not picked by eye. Colour follows
 * the series, never its rank, so a chart with fewer months or a filtered view
 * never repaints the ones that remain.
 */
export const REVENUE = "#3987e5";
export const EXPENSE = "#d95926";
