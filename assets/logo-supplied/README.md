# Supplied marks

Marks chosen by the site's owner. Anything here wins over both the bulk logo set
and the vetted overrides in `assets/logo-overrides`, and the catalogue generator
picks it up by filename: `<catalogue-slug>.<ext>`.

Use this directory when the owner wants a specific image for a service —
including when it differs from the vendor's official mark.
